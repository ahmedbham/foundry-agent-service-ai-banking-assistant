# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: foundry-agent-banking-assistant
metadata:
  template: foundry-agent-banking-assistant

services:
  backend:
    project: ./backend
    host: containerapp
    language: python
    docker:
      path: ./Dockerfile
      remoteBuild: true
  middle:
    project: ./middle
    host: containerapp
    language: python
    docker:
      path: ./Dockerfile
      remoteBuild: true
  frontend:
    project: ./frontend
    host: containerapp
    language: js
    docker:
      path: ./Dockerfile
      remoteBuild: true
